namespace CvManager;

// Marker type for IStringLocalizer<SharedResource>. It stays empty - the
// localizer only needs the type's name to find Resources/SharedResource.*.resx.
// Keys are the English strings themselves, so when a translation is missing the
// localizer returns the key and the UI is still readable English.
public class SharedResource
{
}
